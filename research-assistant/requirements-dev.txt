-r requirements.txt
pytest==8.4.2
pytest-cov==7.0.0
pytest-mock==3.15.1
reportlab==4.4.4   # generates sample PDFs for integration tests
# Caps any single test; local CPU inference can otherwise hang a run for hours.
pytest-timeout==2.4.0
